---
import BaseLayout from '../layouts/BaseLayout.astro';

const lastUpdated = '2026-05-05';
const lovMailto = 'mailto:support@liv4all.com?subject=Letter%20of%20Validation%20request';
const securityMailto = 'mailto:security@liv4all.com';

const dataCards = [
  {
    id: 'c1',
    headline: 'Your Google tokens never touch our application database.',
    body:
      'Tokens are written to a per-user encrypted secret store, separate from the database that holds your account record.',
  },
  {
    id: 'c2',
    headline: 'Tokens are never returned to your browser.',
    body:
      'The dashboard shows only the names of vault entries, never the values. The same applies to OAuth tokens — they are used by the agent and never round-tripped through the browser.',
  },
  {
    id: 'c3',
    headline: 'Modern TLS from your browser to Liv.',
    body:
      'We accept TLS 1.3 only, redirect HTTP to HTTPS, and ship HSTS. Certificate-chain and load-balancer details are audited as part of the assessment rather than published here.',
  },
  {
    id: 'c4',
    headline: 'We hold the minimum Google scopes we need.',
    body:
      'calendar.readonly, calendar.events, contacts.readonly, gmail.modify — nothing else. These are the same scopes Google shows you on the consent screen.',
  },
  {
    id: 'c5',
    headline: 'You can disconnect Google in one click.',
    body:
      "The disconnect button calls Google's revoke endpoint and removes the corresponding secret from our store in the same request.",
  },
];

const subprocessors = [
  {
    name: 'AWS',
    purpose: 'Cloud hosting, storage, secret management, transactional email',
    region: 'Australia (Sydney)',
    data: 'Account data, OAuth tokens, operational logs',
  },
  {
    name: 'Google',
    purpose: 'OAuth + Gmail / Calendar / Contacts APIs the operator authorises',
    region: 'Google global',
    data: 'OAuth tokens, operator email',
  },
  {
    name: 'Telegram',
    purpose: "Operator's chat channel",
    region: 'Telegram global',
    data: 'Operator chat IDs and messages',
  },
  {
    name: 'Anthropic',
    purpose: 'LLM inference (Claude)',
    region: 'Anthropic global',
    data: 'Conversation content',
  },
  {
    name: 'AWS Bedrock',
    purpose: 'LLM inference (Claude, fallback)',
    region: 'Australia or US',
    data: 'Conversation content',
  },
  {
    name: 'Browserbase',
    purpose: "Headless web browsing on the operator's behalf",
    region: 'US',
    data: 'URLs fetched',
  },
  {
    name: 'Brave Search',
    purpose: 'Web search',
    region: 'US',
    data: 'Search queries',
  },
  {
    name: 'TAC Security',
    purpose: 'CASA Tier 2 assessor',
    region: 'Global',
    data: 'No runtime data; sees code and staging during the audit window only',
  },
];
---
<BaseLayout
  title="Trust — Liv4All"
  description="How Liv handles your Gmail, calendar, and personal API keys. Independently assessed under Google's CASA Tier 2 framework by TAC Security."
  ogUrl="https://liv4all.com/trust"
>
  <div class="trust-page">
    <!-- Section A — Hero / Compliance summary -->
    <header class="hero">
      <div class="container-trust">
        <div class="eyebrow fade-in">// trust &amp; security</div>

        <h1 class="fade-in fade-in-delay-1">independently assessed.<br />every 12 months.</h1>

        <p class="hero-sub fade-in fade-in-delay-2">
          TAC Security CASA Tier 2 · Google OAuth verified · Data hosted in Australia.
        </p>

        <div class="badge-row fade-in fade-in-delay-2">
          <div class="badge-card badge-card-primary">
            <img
              src="/tac-esof-casa-tier2-badge.png"
              alt="TAC Security ESOF — Verified and Secured by ADA CASA Assessor"
              width="120"
              height="138"
              loading="eager"
            />
            <div class="badge-meta">
              <div class="badge-name">CASA Tier 2</div>
              <div class="badge-detail">TAC Security · Letter of Validation</div>
            </div>
          </div>

          <div class="badge-card">
            <div class="badge-icon" aria-hidden="true">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"><path d="M12 2 4 6v6c0 5 3.5 9 8 10 4.5-1 8-5 8-10V6l-8-4Z"/><path d="m9 12 2 2 4-4"/></svg>
            </div>
            <div class="badge-meta">
              <div class="badge-name">OAuth verified</div>
              <div class="badge-detail">Google CASA-required scopes</div>
            </div>
          </div>

          <div class="badge-card">
            <div class="badge-icon" aria-hidden="true">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="9"/><path d="M3.5 12h17M12 3.5c2.5 2.7 4 6 4 8.5s-1.5 5.8-4 8.5C9.5 17.8 8 14.5 8 12s1.5-5.8 4-8.5Z"/></svg>
            </div>
            <div class="badge-meta">
              <div class="badge-name">Data in Australia</div>
              <div class="badge-detail">AWS ap-southeast-2 (Sydney)</div>
            </div>
          </div>
        </div>

        <p class="badge-caption fade-in fade-in-delay-3">
          What we're assessed against: Google's <strong>Cloud Application Security Assessment</strong>,
          aligned to OWASP ASVS V1–V14. CASA Tier 2 is a <strong>Letter of Validation</strong>, valid 12 months —
          not a SOC 2 or ISO 27001 certification, both of which are on our path to GA.
        </p>

        <div class="hero-coverage fade-in fade-in-delay-3">
          <div class="hero-coverage-label">// what this covers</div>
          <ul>
            <li>The Liv web app at <code>app.liv4all.com</code> — the surface that holds your tokens.</li>
            <li>The full Google OAuth lifecycle — consent, storage, use, revocation.</li>
            <li>Application security controls per OWASP ASVS V1–V14.</li>
          </ul>
        </div>

        <div class="hero-ctas fade-in fade-in-delay-4">
          <a href={lovMailto} class="cta cta-primary">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M4 6h16v12H4z"/><path d="m4 7 8 6 8-6"/></svg>
            Letter of Validation available on request
          </a>
          <a href="/security" class="cta cta-secondary">Read our Security Policy →</a>
        </div>
      </div>
    </header>

    <!-- Sticky in-page nav -->
    <nav class="trust-nav" aria-label="On this page">
      <div class="container-trust trust-nav-inner">
        <a href="#compliance">Compliance</a>
        <a href="#data">Data</a>
        <a href="#architecture">Architecture</a>
        <a href="#ai-safety">AI safety</a>
        <a href="#sub-processors">Sub-processors</a>
        <a href="#disclosure">Disclosure</a>
      </div>
    </nav>

    <!-- Section B — What CASA Tier 2 actually means -->
    <section id="compliance" class="section">
      <div class="container-trust">
        <div class="section-label fade-in">// section B · compliance</div>
        <h2 class="fade-in">what CASA Tier 2 actually means</h2>

        <div class="prose fade-in">
          <p>
            <strong>CASA</strong> (Cloud Application Security Assessment) Tier 2 is the security review
            Google requires for any app that handles <strong>restricted</strong> Gmail scopes. It's an
            OWASP-ASVS-aligned assessment performed by a Google-authorised lab and includes static
            analysis (SAST), dynamic analysis (DAST), and a manual architecture review.
          </p>
          <p>
            Liv reads and drafts email on your behalf. Google rightly classifies that as a restricted
            scope, so an independent lab — <strong>TAC Security</strong> — assessed our app,
            infrastructure, and data flows end-to-end. The Letter of Validation is available on request:
            email <a href={lovMailto}>support@liv4all.com</a> and we'll reply with the document.
          </p>
          <p>
            What that means for you: an outside party — not Liv's founders — verified that the way we
            store your Google tokens, the way we isolate your data from other users', and the way we
            expose our app to the internet meet a published industry bar. The Letter of Validation is
            re-issued every 12 months for as long as we keep that bar.
          </p>
        </div>

        <div class="flow fade-in" aria-label="Assessment scope: your browser to Google APIs">
          <div class="flow-label">// assessment scope</div>
          <div class="flow-row">
            <div class="flow-node">
              <div class="flow-node-icon" aria-hidden="true">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="4" width="18" height="14" rx="2"/><path d="M3 8h18"/></svg>
              </div>
              <div class="flow-node-label">your browser</div>
            </div>
            <div class="flow-arrow" aria-hidden="true">→</div>
            <div class="flow-node">
              <div class="flow-node-icon" aria-hidden="true">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"><path d="M12 2v6m0 8v6M2 12h6m8 0h6M5 5l4 4m6 6 4 4M5 19l4-4m6-6 4-4"/></svg>
              </div>
              <div class="flow-node-label">Liv's edge</div>
            </div>
            <div class="flow-arrow" aria-hidden="true">→</div>
            <div class="flow-node">
              <div class="flow-node-icon" aria-hidden="true">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="3" width="18" height="18" rx="2"/><path d="M3 9h18M9 21V9"/></svg>
              </div>
              <div class="flow-node-label">Liv's web app</div>
            </div>
            <div class="flow-arrow" aria-hidden="true">→</div>
            <div class="flow-node flow-node-emphasis">
              <div class="flow-node-icon" aria-hidden="true">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"><rect x="4" y="10" width="16" height="11" rx="2"/><path d="M8 10V7a4 4 0 0 1 8 0v3"/></svg>
              </div>
              <div class="flow-node-label">encrypted secret store</div>
            </div>
            <div class="flow-arrow" aria-hidden="true">→</div>
            <div class="flow-node">
              <div class="flow-node-icon" aria-hidden="true">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="9"/><path d="M3.5 12h17M12 3.5c2.5 2.7 4 6 4 8.5s-1.5 5.8-4 8.5C9.5 17.8 8 14.5 8 12s1.5-5.8 4-8.5Z"/></svg>
              </div>
              <div class="flow-node-label">Google APIs</div>
            </div>
          </div>
          <p class="flow-caption">
            Every hop above — including the boundary between the web app and the secret store — was
            in scope for the assessment.
          </p>
        </div>
      </div>
    </section>

    <!-- Section C — How we handle your data -->
    <section id="data" class="section section-alt">
      <div class="container-trust">
        <div class="section-label fade-in">// section C · data handling</div>
        <h2 class="fade-in">how we handle your data</h2>
        <p class="section-sub fade-in">
          Five answers to the questions a security-conscious person actually asks before connecting Gmail.
        </p>

        <div class="card-grid fade-in">
          {dataCards.map((card) => (
            <article class="data-card">
              <div class="data-card-id">{card.id.toUpperCase()}</div>
              <h3>{card.headline}</h3>
              <p>{card.body}</p>
            </article>
          ))}
        </div>
      </div>
    </section>

    <!-- Section D — Architecture & isolation -->
    <section id="architecture" class="section">
      <div class="container-trust">
        <div class="section-label fade-in">// section D · architecture</div>
        <h2 class="fade-in">architecture &amp; isolation</h2>
        <p class="section-sub fade-in">
          Two design properties carry most of the weight here. Both are enforced by the surrounding
          infrastructure, not by the application — the app couldn't break them by accident if it tried.
        </p>

        <div class="panel fade-in">
          <div class="panel-tag">D1</div>
          <h3>your data lives in your own walled garden</h3>
          <ul class="panel-list">
            <li>
              <strong>One operator, one runtime.</strong> Every operator gets their own dedicated agent
              runtime. Code execution, memory, and storage are not shared with any other operator.
            </li>
            <li>
              <strong>Least-privilege identity per operator.</strong> Each runtime carries permissions
              scoped exclusively to that operator's mailbox prefix and outbound mail address. Cross-user
              reads and cross-user sends are rejected at the infrastructure layer, not just the
              application layer.
            </li>
            <li>
              <strong>Filesystem isolation, enforced below the application.</strong> An operator's files
              are only visible inside that operator's runtime. The boundary is enforced by the platform
              itself — the application can't accidentally step over it because the surrounding
              infrastructure won't allow it.
            </li>
            <li>
              <strong>No public ingress to the data plane.</strong> Internet traffic terminates at our
              edge; the database, secret store, and agent runtimes have no public network endpoints.
            </li>
          </ul>
        </div>

        <div class="panel fade-in">
          <div class="panel-tag">D2</div>
          <h3>secrets the agent itself cannot read</h3>
          <ul class="panel-list">
            <li>
              The agent process is <strong>denied direct access to user vault entries</strong> at the
              platform layer; it cannot fetch them on its own.
            </li>
            <li>
              When an outbound call genuinely needs a credential, a separate broker resolves it on the
              agent's behalf — only for the duration of that one call, and only for the operator that
              owns it.
            </li>
            <li>
              A prompt-injected agent that tries to dump its own environment or read its own files finds
              <em>nothing useful</em>. The credentials aren't there to begin with.
            </li>
          </ul>
        </div>
      </div>
    </section>

    <!-- Section E — AI-specific safety -->
    <section id="ai-safety" class="section section-alt">
      <div class="container-trust">
        <div class="section-label fade-in">// section E · AI safety</div>
        <h2 class="fade-in">AI-specific safety</h2>
        <p class="section-sub fade-in">
          The failure modes people fear with an AI agent are different from a normal SaaS product —
          a runaway loop, an injected prompt, a confidently-wrong send. We treat them as a real risk
          class and design around them.
        </p>

        <div class="ai-grid">
          <article class="ai-card fade-in">
            <div class="ai-card-tag">draft-first email</div>
            <p>
              Liv never silently sends email. Every outbound message is drafted and surfaced to you in
              Telegram for explicit approval. No surprise sends, ever.
            </p>
          </article>

          <article class="ai-card fade-in">
            <div class="ai-card-tag">allowlisted Telegram</div>
            <p>
              Each user's bot only accepts messages from the owner's Telegram user ID. Strangers
              messaging the bot are dropped — bounding the blast radius of any attempt to social-engineer
              the agent through its own messaging channel.
            </p>
          </article>

          <article class="ai-card fade-in">
            <div class="ai-card-tag">per-user spend caps</div>
            <p>
              Every operator's agent runs against a hard per-operator inference budget. A runaway loop
              or hostile prompt cannot rack up unbounded model spend — not on someone else's account,
              not on Liv's.
            </p>
          </article>

          <article class="ai-card fade-in">
            <div class="ai-card-tag">prompt injection — modelled, not ignored</div>
            <p>
              Indirect prompt injection (adversarial content in emails, web pages, or calendar invites
              the agent reads) is a real risk class. We mitigate it with the properties above:
              human-in-the-loop on outbound mail, allowlisted messaging, and least-privilege secret
              access. Defence-in-depth is the only honest answer.
            </p>
          </article>
        </div>
      </div>
    </section>

    <!-- Section F — Sub-processors -->
    <section id="sub-processors" class="section">
      <div class="container-trust">
        <div class="section-label fade-in">// section F · sub-processors</div>
        <h2 class="fade-in">sub-processors</h2>
        <p class="section-sub fade-in">
          Vendors that process operator data on Liv's behalf. We list the vendor and the region — not
          the specific managed services we use inside each vendor.
        </p>

        <div class="table-wrap fade-in">
          <table class="trust-table">
            <thead>
              <tr>
                <th>Sub-processor</th>
                <th>Purpose</th>
                <th>Region</th>
                <th>Data class</th>
              </tr>
            </thead>
            <tbody>
              {subprocessors.map((row) => (
                <tr>
                  <td><strong>{row.name}</strong></td>
                  <td>{row.purpose}</td>
                  <td>{row.region}</td>
                  <td>{row.data}</td>
                </tr>
              ))}
            </tbody>
          </table>
        </div>

        <div class="commitment fade-in">
          <div class="commitment-label">// notification commitment</div>
          <p>
            We commit to <strong>30 days advance notice</strong> before adding any new sub-processor
            that processes operator data, with one carve-out: where a change is required for
            <strong>service continuity</strong> — for example, a primary provider becoming
            unavailable — we will notify affected operators within <strong>7 days of the change</strong>.
            Notifications are sent by email to the address on file for each active operator account.
          </p>
        </div>

        <p class="section-aside fade-in">
          See our <a href="/privacy">privacy policy</a> for the full data-handling story, including
          retention and your rights.
        </p>
      </div>
    </section>

    <!-- Section G — Disclosure -->
    <section id="disclosure" class="section section-alt">
      <div class="container-trust">
        <div class="section-label fade-in">// section G · disclosure</div>
        <h2 class="fade-in">vulnerability disclosure</h2>
        <p class="section-sub fade-in">
          Found something? We'd like to hear from you. Researchers acting in good faith will not be
          pursued — see the safe-harbour clause below.
        </p>

        <div class="disclosure-grid">
          <div class="disclosure-card fade-in">
            <div class="disclosure-card-label">// contact</div>
            <h3>security@liv4all.com</h3>
            <p>
              Please email <a href={securityMailto}>security@liv4all.com</a> with a description of the
              issue, reproduction steps, and any artefacts (logs, screenshots, proof-of-concept) you
              can share. PGP-encrypted reports welcome on request.
            </p>
          </div>

          <div class="disclosure-card fade-in">
            <div class="disclosure-card-label">// response window</div>
            <h3>3 business days to acknowledge</h3>
            <p>
              We'll acknowledge a report within <strong>3 business days</strong> and provide an initial
              assessment within <strong>10 business days</strong>. Severity-driven remediation timelines
              follow from there, and we'll keep you in the loop.
            </p>
          </div>

          <div class="disclosure-card disclosure-card-wide fade-in">
            <div class="disclosure-card-label">// safe harbour</div>
            <p>
              We will not pursue legal action against researchers who, in good faith, follow this
              policy: avoid privacy violations, destruction of data, and degradation of service; only
              interact with accounts you own or have explicit permission to test; give us reasonable
              time to remediate before public disclosure; and don't exploit any vulnerability beyond
              what's necessary to demonstrate it.
            </p>
            <p>
              Out of scope: denial-of-service, social-engineering of staff, physical attacks on
              infrastructure, and findings on third-party services where Liv has no remediation path.
            </p>
          </div>
        </div>
      </div>
    </section>

    <!-- Section H — FAQ / Resources -->
    <section id="faq" class="section">
      <div class="container-trust">
        <div class="section-label fade-in">// section H · resources</div>
        <h2 class="fade-in">frequently asked</h2>

        <div class="trust-faq fade-in">
          <details>
            <summary>Is Liv SOC 2 certified?</summary>
            <p>
              Not yet. CASA Tier 2 covers the OAuth-touching application surface per OWASP ASVS.
              SOC 2 Type II is on our path to GA.
            </p>
          </details>

          <details>
            <summary>Where is my data stored?</summary>
            <p>
              Australia (Sydney). We do not currently offer multi-region residency.
            </p>
          </details>

          <details>
            <summary>Can I export or delete my data?</summary>
            <p>
              Yes. Disconnecting Google revokes the tokens with Google and removes the corresponding
              entries from our secret store. A full account-deletion request to
              <a href="mailto:support@liv4all.com">support@liv4all.com</a> tears down your account
              record, your dedicated agent runtime, and every credential we hold for you. Operational
              logs are retained for the period documented in our
              <a href="/privacy">privacy policy</a>.
            </p>
          </details>

          <details>
            <summary>What model does Liv use?</summary>
            <p>
              Claude (the latest GA Sonnet model from Anthropic). We do not train models on your data;
              Anthropic's enterprise terms apply.
            </p>
          </details>

          <details>
            <summary>Does the embed widget see my dashboard data?</summary>
            <p>
              No. The public widget runs against a separate, isolated knowledge-base service that has
              no access to operator data.
            </p>
          </details>

          <details>
            <summary>How often is the Letter of Validation refreshed?</summary>
            <p>
              Annually. We've set a calendar reminder ten months out so the re-scan is in flight before
              the current LoV expires.
            </p>
          </details>
        </div>
      </div>
    </section>

    <!-- Section I — Page footer -->
    <div class="trust-bottom">
      <div class="container-trust trust-bottom-inner">
        <span>Last updated <time datetime={lastUpdated}>{lastUpdated}</time></span>
        <span class="trust-bottom-sep" aria-hidden="true">·</span>
        <a href={lovMailto}>Request the Letter of Validation</a>
        <span class="trust-bottom-sep" aria-hidden="true">·</span>
        <a href="/security">Security policy</a>
        <span class="trust-bottom-sep" aria-hidden="true">·</span>
        <a href="/.well-known/security.txt">security.txt</a>
      </div>
    </div>
  </div>
</BaseLayout>

<style>
  .trust-page { padding-top: 80px; }

  .container-trust {
    width: min(1100px, 100%);
    margin: 0 auto;
    padding: 0 24px;
  }

  /* ── Hero ── */
  .hero {
    padding: 56px 0 40px;
  }

  .eyebrow {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.72rem;
    color: var(--accent);
    letter-spacing: 0.15em;
    text-transform: uppercase;
    display: inline-block;
    padding: 6px 14px;
    background: var(--accent-glow);
    border: 1px solid rgba(139, 92, 246, 0.15);
    border-radius: 100px;
    margin-bottom: 28px;
  }

  h1 {
    font-size: clamp(2.2rem, 5vw, 3.4rem);
    font-weight: 700;
    letter-spacing: -0.03em;
    line-height: 1.1;
    margin-bottom: 16px;
    color: var(--text);
  }

  .hero-sub {
    font-size: 1.05rem;
    color: var(--text-dim);
    max-width: 640px;
    margin-bottom: 40px;
  }

  /* ── Badge row ── */
  .badge-row {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 16px;
    margin-bottom: 24px;
  }

  @media (max-width: 720px) {
    .badge-row { grid-template-columns: 1fr; }
  }

  .badge-card {
    display: flex;
    align-items: center;
    gap: 16px;
    padding: 20px 22px;
    background: var(--surface-glass);
    backdrop-filter: blur(10px);
    -webkit-backdrop-filter: blur(10px);
    border: 1px solid var(--border-light);
    border-radius: var(--radius);
    transition: border-color 0.3s, transform 0.3s, box-shadow 0.3s;
  }

  .badge-card:hover {
    border-color: rgba(139, 92, 246, 0.3);
    transform: translateY(-2px);
    box-shadow: 0 16px 40px rgba(0, 0, 0, 0.25);
  }

  .badge-card-primary {
    border-color: rgba(139, 92, 246, 0.25);
    background: linear-gradient(140deg, rgba(139, 92, 246, 0.08), rgba(18, 18, 26, 0.6));
  }

  .badge-card img {
    display: block;
    width: 56px;
    height: auto;
    object-fit: contain;
    flex-shrink: 0;
  }

  .badge-icon {
    width: 44px;
    height: 44px;
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--accent);
    background: var(--accent-glow);
    border: 1px solid rgba(139, 92, 246, 0.2);
    border-radius: 12px;
    flex-shrink: 0;
  }

  .badge-icon svg { width: 22px; height: 22px; }

  .badge-meta { min-width: 0; }
  .badge-name {
    font-weight: 600;
    font-size: 0.95rem;
    color: var(--text);
    margin-bottom: 2px;
    letter-spacing: -0.01em;
  }
  .badge-detail {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.72rem;
    color: var(--text-dim);
  }

  .badge-caption {
    font-size: 0.88rem;
    color: var(--text-dim);
    max-width: 720px;
    margin-bottom: 36px;
    line-height: 1.7;
  }
  .badge-caption strong { color: var(--text); font-weight: 500; }

  /* ── Hero coverage ── */
  .hero-coverage {
    background: var(--surface-glass);
    backdrop-filter: blur(10px);
    -webkit-backdrop-filter: blur(10px);
    border: 1px solid var(--border-light);
    border-radius: var(--radius);
    padding: 24px 26px;
    margin-bottom: 36px;
  }

  .hero-coverage-label {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.7rem;
    color: var(--text-dim);
    letter-spacing: 0.1em;
    text-transform: uppercase;
    margin-bottom: 12px;
  }

  .hero-coverage ul {
    list-style: none;
    padding: 0;
    margin: 0;
    display: grid;
    gap: 8px;
  }

  .hero-coverage li {
    font-size: 0.92rem;
    color: var(--text-dim);
    padding-left: 22px;
    position: relative;
  }

  .hero-coverage li::before {
    content: '';
    position: absolute;
    left: 0;
    top: 0.6em;
    width: 8px;
    height: 8px;
    border: 1.5px solid var(--accent);
    border-radius: 50%;
    box-shadow: 0 0 12px var(--accent-glow);
  }

  .hero-coverage code {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.85em;
    color: var(--text);
    background: rgba(139, 92, 246, 0.1);
    padding: 2px 6px;
    border-radius: 4px;
    border: 1px solid var(--border-light);
  }

  /* ── CTAs ── */
  .hero-ctas {
    display: flex;
    gap: 12px;
    flex-wrap: wrap;
  }

  .cta {
    display: inline-flex;
    align-items: center;
    gap: 10px;
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.82rem;
    padding: 14px 22px;
    border-radius: 100px;
    transition: all 0.25s;
    border: 1px solid transparent;
  }

  .cta svg { width: 16px; height: 16px; }

  .cta-primary {
    background: var(--accent);
    color: #fff !important;
    box-shadow: 0 6px 24px rgba(139, 92, 246, 0.25);
  }
  .cta-primary:hover {
    background: #7c3aed;
    box-shadow: 0 8px 32px rgba(139, 92, 246, 0.45);
    text-decoration: none !important;
    transform: translateY(-1px);
  }

  .cta-secondary {
    background: transparent;
    color: var(--text);
    border-color: var(--border-light);
  }
  .cta-secondary:hover {
    border-color: var(--accent);
    color: var(--accent);
    text-decoration: none !important;
  }

  /* ── Sticky in-page nav ── */
  .trust-nav {
    position: sticky;
    top: 0;
    z-index: 50;
    background: rgba(10, 10, 15, 0.85);
    backdrop-filter: blur(16px);
    -webkit-backdrop-filter: blur(16px);
    border-top: 1px solid var(--border-light);
    border-bottom: 1px solid var(--border-light);
    margin-top: 32px;
  }

  .trust-nav-inner {
    display: flex;
    gap: 28px;
    overflow-x: auto;
    padding-top: 14px;
    padding-bottom: 14px;
    scrollbar-width: none;
  }

  .trust-nav-inner::-webkit-scrollbar { display: none; }

  .trust-nav a {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.78rem;
    color: var(--text-dim);
    white-space: nowrap;
    transition: color 0.2s;
  }

  .trust-nav a:hover { color: var(--text); text-decoration: none; }

  /* ── Sections ── */
  .section { padding: 72px 0; }
  .section-alt { background: rgba(255, 255, 255, 0.015); border-top: 1px solid var(--border-light); border-bottom: 1px solid var(--border-light); }

  .section-label {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.72rem;
    color: var(--accent);
    letter-spacing: 0.15em;
    text-transform: uppercase;
    display: inline-block;
    padding: 6px 14px;
    background: var(--accent-glow);
    border: 1px solid rgba(139, 92, 246, 0.15);
    border-radius: 100px;
    margin-bottom: 16px;
  }

  h2 {
    font-size: clamp(1.5rem, 3vw, 1.9rem);
    font-weight: 600;
    letter-spacing: -0.02em;
    margin-bottom: 12px;
    color: var(--text);
  }

  .section-sub {
    font-size: 0.95rem;
    color: var(--text-dim);
    max-width: 700px;
    margin-bottom: 36px;
    line-height: 1.7;
  }

  .section-aside {
    font-size: 0.88rem;
    color: var(--text-dim);
    margin-top: 24px;
  }

  /* ── Prose ── */
  .prose { max-width: 760px; margin-bottom: 48px; }
  .prose p { font-size: 0.98rem; color: var(--text-dim); line-height: 1.8; margin-bottom: 16px; }
  .prose strong { color: var(--text); font-weight: 500; }
  .prose a { color: var(--accent); }

  /* ── Flow diagram ── */
  .flow {
    background: var(--surface-glass);
    backdrop-filter: blur(10px);
    -webkit-backdrop-filter: blur(10px);
    border: 1px solid var(--border-light);
    border-radius: var(--radius-lg);
    padding: 28px;
  }

  .flow-label {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.7rem;
    color: var(--text-dim);
    letter-spacing: 0.1em;
    text-transform: uppercase;
    margin-bottom: 20px;
  }

  .flow-row {
    display: flex;
    align-items: stretch;
    gap: 8px;
    overflow-x: auto;
    padding-bottom: 8px;
    scrollbar-width: thin;
  }

  .flow-node {
    flex: 1 1 0;
    min-width: 130px;
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    padding: 18px 12px;
    background: var(--bg);
    border: 1px solid var(--border);
    border-radius: var(--radius);
  }

  .flow-node-emphasis {
    border-color: rgba(139, 92, 246, 0.4);
    background: linear-gradient(140deg, rgba(139, 92, 246, 0.08), var(--bg));
    box-shadow: 0 0 32px rgba(139, 92, 246, 0.08);
  }

  .flow-node-icon {
    width: 36px;
    height: 36px;
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--accent);
    margin-bottom: 10px;
  }

  .flow-node-icon svg { width: 22px; height: 22px; }

  .flow-node-label {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.74rem;
    color: var(--text);
    line-height: 1.3;
  }

  .flow-arrow {
    display: flex;
    align-items: center;
    color: var(--text-dim);
    font-size: 1.1rem;
  }

  .flow-caption {
    font-size: 0.85rem;
    color: var(--text-dim);
    margin-top: 16px;
    line-height: 1.6;
  }

  @media (max-width: 720px) {
    .flow-row { flex-direction: column; align-items: stretch; }
    .flow-arrow { justify-content: center; transform: rotate(90deg); padding: 4px 0; }
    .flow-node { min-width: 0; }
  }

  /* ── Data cards ── */
  .card-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 16px;
  }

  .data-card {
    position: relative;
    background: var(--surface-glass);
    backdrop-filter: blur(10px);
    -webkit-backdrop-filter: blur(10px);
    border: 1px solid var(--border-light);
    border-radius: var(--radius-lg);
    padding: 26px;
    transition: border-color 0.3s, transform 0.3s, box-shadow 0.3s;
  }

  .data-card:hover {
    border-color: rgba(139, 92, 246, 0.3);
    transform: translateY(-3px);
    box-shadow: 0 16px 48px rgba(0, 0, 0, 0.3), 0 0 32px var(--accent-glow);
  }

  .data-card-id {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.7rem;
    color: var(--accent);
    letter-spacing: 0.12em;
    margin-bottom: 12px;
  }

  .data-card h3 {
    font-size: 1rem;
    font-weight: 600;
    color: var(--text);
    line-height: 1.4;
    margin-bottom: 10px;
    letter-spacing: -0.01em;
  }

  .data-card p {
    font-size: 0.9rem;
    color: var(--text-dim);
    line-height: 1.65;
  }

  /* ── Architecture panels ── */
  .panel {
    background: var(--surface-glass);
    backdrop-filter: blur(10px);
    -webkit-backdrop-filter: blur(10px);
    border: 1px solid var(--border-light);
    border-radius: var(--radius-lg);
    padding: 32px;
    margin-bottom: 20px;
    position: relative;
  }

  .panel-tag {
    position: absolute;
    top: 24px;
    right: 24px;
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.7rem;
    color: var(--accent);
    letter-spacing: 0.12em;
    padding: 4px 10px;
    background: var(--accent-glow);
    border: 1px solid rgba(139, 92, 246, 0.2);
    border-radius: 100px;
  }

  .panel h3 {
    font-size: 1.2rem;
    font-weight: 600;
    color: var(--text);
    letter-spacing: -0.01em;
    margin-bottom: 18px;
    padding-right: 60px;
  }

  .panel-list {
    list-style: none;
    padding: 0;
    margin: 0;
    display: grid;
    gap: 14px;
  }

  .panel-list li {
    position: relative;
    padding-left: 22px;
    font-size: 0.93rem;
    color: var(--text-dim);
    line-height: 1.7;
  }

  .panel-list li::before {
    content: '';
    position: absolute;
    left: 0;
    top: 0.65em;
    width: 8px;
    height: 8px;
    border: 1.5px solid var(--accent);
    border-radius: 50%;
  }

  .panel-list strong { color: var(--text); font-weight: 500; }
  .panel-list em { color: var(--text); font-style: italic; }

  /* ── AI safety cards ── */
  .ai-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 16px;
  }

  .ai-card {
    background: var(--surface-glass);
    backdrop-filter: blur(10px);
    -webkit-backdrop-filter: blur(10px);
    border: 1px solid var(--border-light);
    border-radius: var(--radius-lg);
    padding: 24px 26px;
    transition: border-color 0.3s, transform 0.3s, box-shadow 0.3s;
  }

  .ai-card:hover {
    border-color: rgba(192, 132, 252, 0.3);
    transform: translateY(-3px);
    box-shadow: 0 16px 48px rgba(0, 0, 0, 0.3);
  }

  .ai-card-tag {
    display: inline-block;
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.7rem;
    color: var(--pink);
    letter-spacing: 0.05em;
    background: rgba(192, 132, 252, 0.08);
    border: 1px solid rgba(192, 132, 252, 0.2);
    padding: 4px 10px;
    border-radius: 100px;
    margin-bottom: 12px;
  }

  .ai-card p {
    font-size: 0.92rem;
    color: var(--text-dim);
    line-height: 1.7;
  }

  /* ── Sub-processors table ── */
  .table-wrap {
    background: var(--surface-glass);
    backdrop-filter: blur(10px);
    -webkit-backdrop-filter: blur(10px);
    border: 1px solid var(--border-light);
    border-radius: var(--radius-lg);
    overflow: hidden;
  }

  .trust-table {
    width: 100%;
    border-collapse: collapse;
    font-size: 0.9rem;
  }

  .trust-table th, .trust-table td {
    text-align: left;
    padding: 14px 18px;
    border-bottom: 1px solid var(--border-light);
    color: var(--text-dim);
    vertical-align: top;
  }

  .trust-table tr:last-child td { border-bottom: 0; }

  .trust-table th {
    color: var(--text);
    font-weight: 500;
    font-size: 0.72rem;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    background: rgba(255, 255, 255, 0.02);
  }

  .trust-table td strong { color: var(--text); font-weight: 500; }

  @media (max-width: 720px) {
    .trust-table { font-size: 0.84rem; }
    .trust-table th, .trust-table td { padding: 12px 14px; }
  }

  .commitment {
    margin-top: 24px;
    padding: 22px 26px;
    background: var(--surface-glass);
    backdrop-filter: blur(10px);
    -webkit-backdrop-filter: blur(10px);
    border: 1px solid var(--border-light);
    border-left: 2px solid var(--accent);
    border-radius: var(--radius);
  }

  .commitment-label {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.7rem;
    color: var(--accent);
    letter-spacing: 0.1em;
    text-transform: uppercase;
    margin-bottom: 10px;
  }

  .commitment p {
    font-size: 0.92rem;
    color: var(--text-dim);
    line-height: 1.7;
    margin: 0;
  }

  .commitment strong { color: var(--text); font-weight: 500; }

  /* ── Disclosure ── */
  .disclosure-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 16px;
  }

  @media (max-width: 720px) {
    .disclosure-grid { grid-template-columns: 1fr; }
  }

  .disclosure-card {
    background: var(--surface-glass);
    backdrop-filter: blur(10px);
    -webkit-backdrop-filter: blur(10px);
    border: 1px solid var(--border-light);
    border-radius: var(--radius-lg);
    padding: 26px;
  }

  .disclosure-card-wide { grid-column: 1 / -1; }

  .disclosure-card-label {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.7rem;
    color: var(--text-dim);
    letter-spacing: 0.1em;
    text-transform: uppercase;
    margin-bottom: 12px;
  }

  .disclosure-card h3 {
    font-family: 'JetBrains Mono', monospace;
    font-size: 1rem;
    font-weight: 500;
    color: var(--text);
    margin-bottom: 12px;
    word-break: break-word;
  }

  .disclosure-card p {
    font-size: 0.92rem;
    color: var(--text-dim);
    line-height: 1.7;
    margin-bottom: 12px;
  }

  .disclosure-card p:last-child { margin-bottom: 0; }
  .disclosure-card strong { color: var(--text); font-weight: 500; }

  /* ── FAQ ── */
  .trust-faq {
    display: grid;
    gap: 10px;
  }

  .trust-faq details {
    background: var(--surface-glass);
    backdrop-filter: blur(10px);
    -webkit-backdrop-filter: blur(10px);
    border: 1px solid var(--border-light);
    border-radius: var(--radius);
    transition: border-color 0.3s;
  }

  .trust-faq details:hover { border-color: rgba(139, 92, 246, 0.25); }

  .trust-faq summary {
    padding: 18px 22px;
    cursor: pointer;
    font-size: 0.95rem;
    font-weight: 500;
    color: var(--text);
    list-style: none;
    display: flex;
    justify-content: space-between;
    align-items: center;
    user-select: none;
    gap: 16px;
  }

  .trust-faq summary::-webkit-details-marker { display: none; }

  .trust-faq summary::after {
    content: '+';
    font-size: 1.2rem;
    color: var(--accent);
    flex-shrink: 0;
    transition: transform 0.2s;
  }

  .trust-faq details[open] summary::after { content: '−'; }

  .trust-faq p {
    padding: 0 22px 22px;
    font-size: 0.92rem;
    color: var(--text-dim);
    line-height: 1.7;
  }

  .trust-faq a { color: var(--accent); }

  /* ── Bottom strip ── */
  .trust-bottom {
    border-top: 1px solid var(--border-light);
    padding: 28px 0;
  }

  .trust-bottom-inner {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    align-items: center;
    gap: 14px;
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.78rem;
    color: var(--text-dim);
  }

  .trust-bottom-inner a { color: var(--text-dim); }
  .trust-bottom-inner a:hover { color: var(--accent); text-decoration: none; }
  .trust-bottom-sep { color: var(--border); }

  @media (max-width: 600px) {
    .section { padding: 56px 0; }
    .panel { padding: 24px; }
    .panel-tag { position: static; display: inline-block; margin-bottom: 12px; }
    .panel h3 { padding-right: 0; }
  }
</style>
